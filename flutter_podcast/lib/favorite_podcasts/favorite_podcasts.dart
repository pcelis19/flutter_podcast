import 'package:flutter/material.dart';

class FavoritePodcasts extends StatelessWidget {
  const FavoritePodcasts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favorite Podcasts'),
    );
  }
}
